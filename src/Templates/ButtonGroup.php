<?php

namespace MBDI\Templates;
defined( 'ABSPATH' ) || die;

/**
 * ButtonGroup is just a select field with multiple values so we can reuse the select template.
 */
class ButtonGroup extends Select {
}
