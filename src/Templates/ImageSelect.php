<?php

namespace MBDI\Templates;
defined( 'ABSPATH' ) || die;

/**
 * ImageSelect is just a select field with different HTML output so we can reuse the select template.
 */
class ImageSelect extends Select {
}
